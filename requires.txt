proxy 
rxjs

Objetos em JavaScript

if (filtros.pageSize) {
    const skip = filtros.pageStart * filtros.pageSize;
        if (skip) {
            query.skip(skip);
        }
        if (filtros.pageSize) {
            query.take(filtros.pageSize);
        }
}

Docker 


API https://docs.exchangeratesapi.com.au/authentication

API_KEY = littlemountain_c0148e8fdc9862a542bc23c424adb258



